sudo: required
language: java
services:
  - docker
before_install:
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"

script: bundle exec ./gradlew assemble -Pbuild-travis=""
